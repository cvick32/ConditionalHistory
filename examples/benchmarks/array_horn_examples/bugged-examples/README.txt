check double swap and check swap have incorrect invariant structure
init_even has 'exists', not sure what to do

all the rest have "BUG" in the name and are duplicates of other
examples and only differ from the non-bugged examples by a few
characters, sometimes just comments
